export function getAIHeuristics(signals: any): string {
  // Proprietary prompt logic (stubbed for MVP)
  // Never expose raw prompts or private data
  return "Based on current signals, this trend shows strong consolidation.";
}
